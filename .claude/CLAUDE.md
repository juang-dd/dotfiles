## Interacting with Github

When interacting with Github (github.com), ALWAYS use `gh` on the command line.

When creating pull requests, always include the line "Written using Claude Code" at the top of the PR description.